/*!
 * PRHONE Applications
 * Central | Database
 * Romel Pérez, 2013
 **/

// ------------------------------------------------------------------------- //
// DATABASE //

// Section
/*var mongodb = require('mongodb');
var mongodbServer = new mongodb.Server('192.168.1.18', 27017, {});
var db = new mongodb.Db('unTestDB', mongodbServer, {});
db.open(function(error, client){
	if(error) throw error;
	var personas = new mongodb.Collection(client, 'personas');
	personas.find({'nombre': 'Romel'});
});*/
